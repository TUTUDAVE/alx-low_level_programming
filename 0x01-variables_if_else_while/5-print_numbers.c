#include <stdio.h>

/**
 *main -printing single digit number of base 10 starting from zero
 *
 * Return: 0 (success)
 */

int main(void)
{
	int f;

	for (f = 0 && f < 10, f++)
	{
		printf("%d", f);
	}

		return (0);

}
