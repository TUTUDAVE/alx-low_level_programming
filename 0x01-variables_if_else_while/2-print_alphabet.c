#include <stdio.h>

/**
 * main - Entry point for printing lowercase letters
 *
 * Return: 0 (success)
 */

int main(void)
{
	putchar("Lowercase letters: \n");

	for (ch = 'a'; ch <= 'z'' ch++)
	{
		putchar ("%c ", ch);
	}

	return (0);
}
